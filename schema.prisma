datasource db {
  url      = env("DATABASE_URL")
  provider = "mysql"
}

generator client {
  provider = "prisma-client-js"
}

model Haipai {
  id        String   @unique
  haipai    String
  createdAt DateTime @default(now()) @map("created_at")
}